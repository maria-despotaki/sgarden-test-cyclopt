# Server
server.port=${PORT:4000}

# MongoDB
spring.data.mongodb.uri=${DATABASE_URL:mongodb://localhost:27017/sgarden}

# JWT
app.jwt.secret=${SERVER_SECRET:sgarden-secret-key}
app.jwt.expiration=86400000

# Logging
logging.level.com.sgarden=DEBUG
spring.output.ansi.enabled=ALWAYS

# Enum handling
spring.jackson.mapper.accept-case-insensitive-enums=true
